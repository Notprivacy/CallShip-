{
  "name": "callship",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "concurrently -n server,dialer -c blue,green \"npm run dev:server\" \"npm run dev:dialer\"",
    "dev:server": "cd server && npm run dev",
    "dev:dialer": "cd dialer && npm run dev",
    "build": "cd dialer && npm install && npm run build && cd .. && node scripts/copy-dialer-build.js && cd server && npm install && cd ..",
    "start": "cd server && node src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
