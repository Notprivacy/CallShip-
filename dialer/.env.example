# URL base de la API (solo para producción cuando el frontend y el backend están en dominios distintos).
# Si el front está en www.callship.us y el backend en Railway, pon aquí la URL del backend + /api.
# Ejemplo: https://callship-production.up.railway.app/api
# Dejar vacío o no definir = usar /api (misma origen; válido si el mismo servidor sirve front y API).
# VITE_API_URL=
